name: Daily Shuttle Stats Image

on:
  schedule:
    - cron: "0 12 * * *"  # every day at 12:00 UTC
  workflow_dispatch:      # let you trigger manually too

jobs:
  run-shuttle-stats:
    runs-on: ubuntu-latest

    permissions:
      contents: write
      actions: write   # needed to upload artifacts

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install requirements
        run: |
          pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run shuttle_stats.py
        run: python shuttle_stats.py

      - name: Commit updated images
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add tt_projects.png tt_utilisation.png
          git diff --cached --quiet || git commit -m "chore: update shuttle stats images"
          git push

      - name: Upload image artifacts
        uses: actions/upload-artifact@v4
        with:
          name: result-images
          path: |
            tt_projects.png
            tt_utilisation.png
          if-no-files-found: error

      - name: Add image to job summary
        run: |
          echo "## Daily Shuttle Stats" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "![Projects](https://raw.githubusercontent.com/TinyTapeout/tt-shuttle-stats/main/tt_projects.png)" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "![Utilisation](https://raw.githubusercontent.com/TinyTapeout/tt-shuttle-stats/main/tt_utilisation.png)" >> $GITHUB_STEP_SUMMARY

